export default function ReportsPage({ params }: { params: { id: string } }) {
    return (
        <div>
            <h1>Reports Page</h1>
            {/* Reports components go here */}
        </div>
    );
}